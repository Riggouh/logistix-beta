version: "3.8"
services:
  logistix:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - logistix-data:/data
    environment:
      - DATA_DIR=/data
    restart: unless-stopped
volumes:
  logistix-data:
